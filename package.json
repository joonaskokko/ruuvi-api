{
	"name": "ruuvi-api",
	"version": "1.0.0",
	"description": "App that gets data from Ruuvi gateway and saves it to an SQL database.",
	"main": "start.ts",
	"engines": {
		"node": ">=25.1.0",
		"npm": ">=11.6.2"
	},
	"scripts": {
		"start": "node --import dotenv-flow/config start.ts --experimental-transform-types",
		"validate": "npx tsc --noEmit",
		"test": "node --import dotenv-flow/config ./test/test.ts --experimental-transform-types"
	},
	"author": "Joonas Kokko",
	"license": "ISC",
	"type": "module",
	"dependencies": {
		"axios": "^1.12.0",
		"date-fns": "^4.1.0",
		"dotenv-flow": "^4.1.0",
		"express": "^4.21.2",
		"knex": "^3.1.0",
		"mysql2": "^3.13.0",
		"node-schedule": "^2.1.1"
	},
	"devDependencies": {
		"@types/express": "^5.0.1",
		"sqlite3": "^5.1.7",
		"typescript": "^5.8.3"
	}
}
